import Layout from '../components/Layout';
export default function Dashboard(){
return (
<Layout>
<h2>My Listings</h2>
<p>List and edit your listings here (query allListings filtered by ownerId via RLS).</p>
</Layout>
);
}