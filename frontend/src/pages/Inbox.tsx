import Layout from '../components/Layout';
export default function Inbox(){
return (
<Layout>
<h2>Inbox</h2>
<p>Simple placeholder: use messages/exchange_requests queries to build threads.</p>
</Layout>
);
}